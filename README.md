# モダンなウェブサイト

HTML、CSS、JavaScriptを使用して作成されたシンプルで美しいウェブサイトです。

## ファイル構成

- `index.html` - メインのHTMLファイル（ページの構造）
- `styles.css` - スタイルシート（デザインとレイアウト）
- `script.js` - JavaScriptファイル（インタラクティブな機能）
- `README.md` - このファイル

## 使い方

1. すべてのファイルを同じフォルダに保存します
2. `index.html`をブラウザで開きます
3. または、ローカルサーバーを起動して表示します

### ローカルサーバーの起動方法

#### Python 3を使用する場合:
```bash
python3 -m http.server 8000
```

#### Node.jsを使用する場合:
```bash
npx http-server
```

その後、ブラウザで `http://localhost:8000` にアクセスします。

## 機能

- ✅ レスポンシブデザイン（モバイル対応）
- ✅ スムーズスクロール
- ✅ モバイルメニュー
- ✅ フォームバリデーション
- ✅ スクロールアニメーション
- ✅ モダンなUIデザイン

## カスタマイズ方法

### 色を変更する
`styles.css`の`:root`セクションで色を変更できます：

```css
:root {
    --primary-color: #6366f1;  /* メインカラー */
    --secondary-color: #8b5cf6;  /* サブカラー */
    /* ... */
}
```

### コンテンツを変更する
`index.html`を編集して、テキストやセクションを変更できます。

### 機能を追加する
`script.js`に新しい機能を追加できます。

## 学習ポイント

このプロジェクトで学べること：

1. **HTML**: セマンティックなHTML構造
2. **CSS**:
   - FlexboxとGridレイアウト
   - CSS変数（カスタムプロパティ）
   - レスポンシブデザイン
   - アニメーション
3. **JavaScript**:
   - DOM操作
   - イベントリスナー
   - フォーム処理
   - Intersection Observer API

## 開発ガイドライン

### Gitコミット時の注意事項

このプロジェクトでは、**コミット前に必ずCHANGELOG.mdを更新する**必要があります。

#### コミット手順

1. 機能実装・変更を行う
2. **CHANGELOG.mdに作業内容を追記**
3. 変更ファイルをステージング: `git add .`
4. コミット: `git commit -m "コミットメッセージ"`

#### Cursorエージェントのルール

このプロジェクトでは、`.cursor/rules/`ディレクトリにルールファイルを配置しています。Cursorエージェントはこれらのルールに従って、コミット前にCHANGELOG.mdの更新を自動的にチェックします。

詳細は`.cursor/rules/commit_rules.mdc`を参照してください。

## 次のステップ

- バックエンドを追加してフォームを実際に送信できるようにする
- より多くのセクションを追加する
- 画像やアイコンを追加する
- より複雑なアニメーションを実装する

